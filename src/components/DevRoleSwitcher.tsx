// DevRoleSwitcher has been removed — role switching was mock-only.
// This file is kept as an empty re-export to avoid breaking any
// remaining imports until they are cleaned up.
export function DevRoleSwitcher() {
  return null;
}
